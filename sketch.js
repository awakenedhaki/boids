// Declaring Global Variables ==================================================
let swarm;

// p5js Boilerplate ============================================================
function setup() {
  createCanvas(1000, 500);
}

function draw() {
  background(0);
}

function keyPressed() {
  if (key === " ") {
    noLoop();
  }
}
